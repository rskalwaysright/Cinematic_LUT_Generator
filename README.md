# 🎬 Cinematic LUT Generator (Python)

A Python-based Cinematic LUT Generator that extracts dominant color palettes from an image or video frame and generates a 3D LUT (.cube) file.  
The LUT can then be applied in professional tools like **DaVinci Resolve**, **Premiere Pro**, or **Photoshop** to replicate cinematic color tones.

---

## 🚀 How to Run

1. (Optional) Place a sample video in `samples/sample_video.mp4` or upload via Streamlit.
2. Install dependencies:
   ```bash
   pip install -r requirements.txt
